services:
  postgres:
    image: postgres:16
    container_name: postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: helpTree
      POSTGRES_PASSWORD: helpTree
      POSTGRES_DB: helpTree_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - helpTree-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U helpTree"]
      interval: 5s
      timeout: 3s
      retries: 10

  prometheus:
    image: prom/prometheus:v2.47.0
    container_name: prometheus_helpTree
    restart: unless-stopped
    volumes:
      - ./helpTree-service/src/main/resources/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=15d'
    ports:
      - '9090:9090'
    networks:
      - helpTree-network
    depends_on:
      - postgres

  grafana:
    image: grafana/grafana:10.2.2
    container_name: grafana_helpTree
    restart: unless-stopped
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
      GF_INSTALL_PLUGINS: ""
      GF_UPDATE_CHECKER_ENABLED: false
    ports:
      - '3000:3000'
    depends_on:
      - prometheus
    networks:
      - helpTree-network
    volumes:
      - grafana_data:/var/lib/grafana

  jaeger:
    image: jaegertracing/all-in-one:1.50
    container_name: jaeger
    restart: unless-stopped
    ports:
      - "16686:16686"  # UI
      - "6831:6831/udp"  # Jaeger agent (thrift compact)
      - "14268:14268"  # Jaeger collector (HTTP)
      - "4317:4317"  # OTLP receiver (gRPC)
      - "4318:4318"  # OTLP receiver (HTTP)
    environment:
      COLLECTOR_OTLP_ENABLED: "true"
      SPAN_STORAGE_TYPE: badger
    networks:
      - helpTree-network
    volumes:
      - jaeger_data:/var/lib/jaeger
    healthcheck:
      test: [ "CMD", "wget", "-q", "--spider", "http://localhost:16686" ]
      interval: 10s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:latest
    container_name: minio
    restart: unless-stopped
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes:
      - minio_data:/data
    networks:
      - helpTree-network
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
    name: helpTree_postgres_data
  prometheus_data:
    name: helpTree_prometheus_data
  grafana_data:
    name: helpTree_grafana_data
  jaeger_data:
    name: helpTree_jaeger_data
  minio_data:
    name: helpTree_minio_data

networks:
  helpTree-network:
    name: helpTree-network
    driver: bridge