import { ThemeProvider, useTheme, darkTheme, lightTheme } from './ThemeContext';
import type { ThemeContextType, ThemeMode } from './ThemeContext';

export { ThemeProvider, useTheme, darkTheme, lightTheme };
export type { ThemeContextType, ThemeMode };
export const theme = darkTheme;
