server:
  port: 8080

spring:
  application:
    name: helpTree
  main:
    allow-bean-definition-overriding: true

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 50MB

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/helpTree_db}
    username: ${DB_USER:helpTree}
    password: ${DB_PASS:helpTree}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  security:
    user:
      name: disabled
      password: disabled

  flyway:
    enabled: true
    baseline-on-migrate: true

# ======================================================
# ТРАССИРОВКА (по аналогии с gateway)
# ======================================================
management:
  tracing:
    enabled: true
    sampling:
      probability: 1.0
    propagation:
      type: w3c
    mdc:
      field-names:
        - traceId
        - spanId
  opentelemetry:
    tracing:
      export:
        otlp:
          endpoint: http://localhost:4318/v1/traces
          protocol: http/protobuf
  metrics:
    export:
      otlp:
        enabled: false
    distribution:
      percentiles-histogram:
        http.server.requests: true
        http.client.requests: true
  endpoints:
    web:
      exposure:
        include:
          - health
          - metrics
          - info
          - prometheus
          - loggers
          - tracing

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.springframework.web: DEBUG
    org.example.helptreeservice: DEBUG
    io.micrometer.tracing: DEBUG
    io.opentelemetry: DEBUG
    io.opentelemetry.exporter.otlp: DEBUG

  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} %highlight(%-5level) [%thread] %green([HELPTREE]) %X{traceId} %X{spanId} %X{requestId} %X{userId} %cyan(%logger{36}) - %msg%n"



app:
  jwtSecret: ${JWT_SECRET:dev-only-secret-not-for-production-64chars-minimum-length!!}
  jwtExpirationMs: 86400000
  cors:
    allowed-origins: ${CORS_ORIGINS:http://localhost:3000,http://localhost:3001,http://localhost:5173}
  admin:
    email: ${ADMIN_EMAIL:admin@helptree.dev}
    password: ${ADMIN_PASSWORD:dev-admin-password-change-in-prod}
  economy:
    coins-per-help: 10
    coins-per-help-received: 2
    coins-per-review: 2
    coins-per-daily-login: 1
    first-help-bonus: 3
    boost-price: 5
    boost-hours: 24
    refresh-token-days: 7
    debt-block-threshold: 5
    debt-warning-threshold: 3
    debt-create-post-threshold: 2
  rate-limit:
    enabled: true
    default-limit: 1000
    auth-limit: 100
    create-limit: 50
    window-seconds: 60

minio:
  endpoint: ${MINIO_ENDPOINT:http://localhost:9000}
  access-key: ${MINIO_ACCESS_KEY:minioadmin}
  secret-key: ${MINIO_SECRET_KEY:minioadmin}
  bucket: ${MINIO_BUCKET:helptree-images}

upload:
  max-size: 10485760  # 10MB в байтах
  allowed-types: image/jpeg,image/png,image/webp,image/gif

resilience4j:
  ratelimiter:
    configs:
      default:
        limitForPeriod: 1000
        limitRefreshPeriod: 60s
        timeoutDuration: 1000ms
      auth:
        limitForPeriod: 100
        limitRefreshPeriod: 60s
        timeoutDuration: 1000ms
      create:
        limitForPeriod: 50
        limitRefreshPeriod: 60s
        timeoutDuration: 1000ms