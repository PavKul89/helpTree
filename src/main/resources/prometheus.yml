global:
  scrape_interval: 5m
  evaluation_interval: 15m

scrape_configs:
  - job_name: "helpTree-app"
    metrics_path: '/actuator/prometheus'

    static_configs:
      - targets: ['host.docker.internal:8080']
      - labels:
          application: 'helpTree-app'
          environment: 'production'